import React, { Component } from 'react';
import Auth from './Auth'
import {database} from './firebaseConfig'

const App = (props) => (
  <div>
  <Auth>
    <div>
      Auth
    </div>
  </Auth>

  </div>

)


export default App;
